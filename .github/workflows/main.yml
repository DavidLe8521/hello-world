name: Deploy on push

on:
  push:
    branches: [master]
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: install ssh keys
        # check this thread to understand why its needed:
        # https://stackoverflow.com/a/70447517
        run: |
          install -m 400 -D /dev/null ~/.ssh/github_action_1
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/github_action_1
          ssh-keyscan -H ${{ secrets.SSH_HOST }} > ~/.ssh/known_hosts
      
      - name: check ssh key
        run: |
          pwd
          ls -al
          ls -al ~/.ssh/
      
      - name: connect and pull
        run: ssh -i ~/.ssh/github_action_1 david@${{ secrets.SSH_HOST }} "ip a"
      #- name: cleanup
       # run: rm -rf ~/.ssh

